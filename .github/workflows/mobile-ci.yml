name: Mobile CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - working-directory: apps/mobile
        run: flutter pub get
      - working-directory: apps/mobile
        run: flutter gen-l10n
      - name: Run Flutter analysis
        id: flutter-analysis
        working-directory: apps/mobile
        shell: bash
        run: |
          set +e
          flutter analyze > flutter-analysis.log 2>&1
          status=$?
          cat flutter-analysis.log
          exit $status
      - name: Upload Flutter analysis diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-analysis-diagnostics
          path: apps/mobile/flutter-analysis.log
          if-no-files-found: error
          retention-days: 1
      - working-directory: apps/mobile
        run: flutter test
