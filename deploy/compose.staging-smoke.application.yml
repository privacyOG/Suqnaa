services:
  api:
    environment:
      NODE_ENV: test
    ports:
      - "127.0.0.1:${SUQNAA_STAGING_SMOKE_API_PORT:-4000}:4000"

  web:
    ports:
      - "127.0.0.1:${SUQNAA_STAGING_SMOKE_WEB_PORT:-3000}:3000"
